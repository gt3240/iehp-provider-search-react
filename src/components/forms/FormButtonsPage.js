import React from 'react';

// Since this component is simple and static, there's no parent container for
// it.
const FormButtonsPage = () => {
  return (
    <div>
      <h2 className="alt-header">Form Buttons Page</h2>   
    </div>
    );
};

export default FormButtonsPage;