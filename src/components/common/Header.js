import React from 'react';
//import {Link, IndexLink} from 'react-router';

const Header = () => {
    return(
        <header className="header">
            <img src="../contents/images/logo.png" alt="logo" />
            <span className="header-title">Style Guide</span>
        </header>
    );
};

export default Header;